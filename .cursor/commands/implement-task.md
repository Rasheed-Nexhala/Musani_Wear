# Implement Task from IMPLEMENTATION_PLAN

Implement the specified task from @Documents/IMPLEMENTATION_PLAN.md.

**Instructions:**
1. Read the full task description and subtasks from IMPLEMENTATION_PLAN
2. Use the appropriate skills:
   - @angular-20 for Angular components, routing, signals
   - @firebase-angular for Firestore, Auth, Storage, rules
   - @musani-design-system for UI, colors, typography
   - @jest-angular for utils/services unit tests
   - @angular-testing-library for component behavior tests
3. Keep components dummy where possible (logic in utils/shared)
4. After implementation, suggest running `/verify-phase` or `/run-tests` as appropriate

**User should specify:** Task number (e.g. 1.1, 2.3, 3.4) in the chat when invoking.
